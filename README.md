# Decision_Intelligence_Assistant
The system answers user questions, retrieves relevant past cases using RAG, predicts ticket priority with both a trained ML model and an LLM, and lets the user compare everything that comes out — in quality, in cost, and in latency.
